description: >
  Check a Django project using a different version of python

usage:
  version: 2.1

  orbs:
    docker: pennlabs/django@x.y.z

  workflows:
    build-and-deploy:
      jobs:
        - django/check:
            project-name: pennlabs
            python-version: "3.8"
