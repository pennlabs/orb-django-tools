description: >
  Check a Django project in the root directory

usage:
  version: 2.1

  orbs:
    django: pennlabs/django-tools@x.y.z

  workflows:
    build-and-deploy:
      jobs:
        - django/check:
            project-name: pennlabs
